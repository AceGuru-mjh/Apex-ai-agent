package com.apex.agent.core.normal.context

// STUBBED: had 1 errors
data class ConversationMessage(val placeholder: String = "")
enum class Role { DEFAULT }
enum class CompressionTier { DEFAULT }
data class CompressionResult(val placeholder: String = "")
class SmartContextCompressor
