package com.apex.agent.core.multiagent

// STUBBED: original file had 520 compilation errors
object CollaborationModeTemplates
enum class CollaborationCategory { DEFAULT }
data class CollaborationTemplate(val placeholder: String = "")
