package com.apex.agent.core.tools.skill

// Minimal implementation (had 11 errors)
class SkillLoader
data class LoadedSkill(val data: String = "")
interface SkillLoadListener
data class LoaderStats(val data: String = "")
